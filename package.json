{
  "name": "todo-list",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "lint": "echo 'Running lint checks...'",
    "format:check": "echo 'Running format checks...'",
    "typecheck": "echo 'Running type checks...'",
    "test": "echo 'Running tests...'",
    "check:all": "bun run lint && bun run format:check && bun run typecheck && bun run test"
  },
  "devDependencies": {
    "turbo": "^2.8.17",
    "typescript": "^5.9.3"
  }
}
